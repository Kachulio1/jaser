interface ISvg {
  splash: string;
  logo: string;
  search: string
}

const svg = {
  splash: require('./splash.svg'),
  logo: require('./logo.svg'),
  search: require('./search.svg'),
  
};

export default svg;
